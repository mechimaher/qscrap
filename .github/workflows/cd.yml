name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: Build & Push to GHCR
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push to GHCR
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build-and-push]
    
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            echo "🏷️ Tagging current as stable for rollback..."
            docker tag qscrap-backend:latest qscrap-backend:stable 2>/dev/null || true
            
            echo "📥 Pulling latest image from GHCR..."
            docker pull ghcr.io/mechimaher/qscrap:latest || true
            
            echo "🔨 Rebuilding backend container..."
            docker stop qscrap-backend 2>/dev/null || true
            docker rm qscrap-backend 2>/dev/null || true
            
            # Restart with existing docker-compose or standalone
            if [ -f /root/docker-compose.yml ]; then
              cd /root && docker compose up -d
            else
              docker run -d --name qscrap-backend \
                --network host \
                -e NODE_ENV=production \
                --restart unless-stopped \
                qscrap-backend:latest || docker start qscrap-backend
            fi
            
            echo "⏳ Waiting for services..."
            sleep 10
            
            echo "📝 Recording deployment..."
            echo "${{ github.sha }}" > /root/.last_good_deploy
            
            echo "✅ Deployment complete!"
            docker ps --filter name=qscrap

      - name: Comprehensive health check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            echo "=== Health Check ==="
            
            # API Health
            if curl -sf http://localhost:3000/health > /dev/null; then
              echo "✅ API: Healthy"
            else
              echo "❌ API: Failed"
              exit 1
            fi
            
            # PostgreSQL
            if docker exec qscrap-postgres pg_isready -U postgres > /dev/null 2>&1; then
              echo "✅ PostgreSQL: Ready"
            else
              echo "❌ PostgreSQL: Not ready"
            fi
            
            # Redis
            if docker exec qscrap-redis redis-cli ping > /dev/null 2>&1; then
              echo "✅ Redis: Ready"
            else
              echo "❌ Redis: Not ready"
            fi
            
            # Disk usage
            echo ""
            echo "=== Disk Usage ==="
            df -h /root | tail -1

      - name: Notify success
        if: success()
        run: |
          echo "✅ Deployment successful!"
          echo "Commit: ${{ github.sha }}"
          echo "Actor: ${{ github.actor }}"
          echo "Time: $(date -u)"
          # Slack notification (if SLACK_WEBHOOK secret is configured)
          if [ -n "${{ secrets.SLACK_WEBHOOK }}" ]; then
            curl -X POST "${{ secrets.SLACK_WEBHOOK }}" \
              -H 'Content-type: application/json' \
              -d '{
                "text": "✅ QScrap deployed successfully",
                "blocks": [{
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*QScrap Deployed* 🚀\nCommit: `${{ github.sha }}`\nBy: ${{ github.actor }}"
                  }
                }]
              }'
          fi

      - name: Notify failure
        if: failure()
        run: |
          echo "❌ Deployment FAILED!"
          echo "Commit: ${{ github.sha }}"
          echo "Actor: ${{ github.actor }}"
          # Slack notification (if SLACK_WEBHOOK secret is configured)
          if [ -n "${{ secrets.SLACK_WEBHOOK }}" ]; then
            curl -X POST "${{ secrets.SLACK_WEBHOOK }}" \
              -H 'Content-type: application/json' \
              -d '{"text": "❌ QScrap deployment FAILED - Commit: ${{ github.sha }} - Actor: ${{ github.actor }}"}'
          fi
